# Linux System Monitor

A simple Python tool to monitor:

- CPU usage
- Memory usage
- Disk usage
- System uptime

## How to Run

Make sure Python and `psutil` are installed:

```bash
pip install psutil
python3 monitor.py
